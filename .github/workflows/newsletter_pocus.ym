name: POCUS Weekly Digest

on:
  schedule:
    - cron: '0 7 * * 5'
  workflow_dispatch:

jobs:
  invia-newsletter-pocus:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Installa Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Installa dipendenze
        run: |
          pip install google-auth-oauthlib google-auth-httplib2 google-api-python-client

      - name: Configura credenziali
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
          GMAIL_USER_POCUS: ${{ secrets.GMAIL_USER_POCUS }}
          GMAIL_TOKEN_POCUS: ${{ secrets.GMAIL_TOKEN_POCUS }}
        run: |
          echo "Credenziali configurate"

      - name: Esegui newsletter POCUS
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
          GMAIL_USER_POCUS: ${{ secrets.GMAIL_USER_POCUS }}
          GMAIL_TOKEN_POCUS: ${{ secrets.GMAIL_TOKEN_POCUS }}
        run: python newsletter_pocus.py
